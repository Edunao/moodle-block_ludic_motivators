define(['jquery', 'core/tree'], function ($, Tree) {
    var timer = {
        init: function () {
            console.log('Timer init');
            var that = this;            
        }
    };
    return timer;
});
